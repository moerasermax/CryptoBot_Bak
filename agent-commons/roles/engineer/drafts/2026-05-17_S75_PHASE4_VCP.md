# S75 Phase 4 完工 VCP — Sidekick Hub (ACP IPC) 整合驗收

> **位階**：Engineer 端暫存（drafts）— 對齊 `~/.agentcharter/core/working-stack-discipline.md §2`
> **存檔時點**：2026-05-17 Phase 4 整合測試 + 完工驗收 VCP
> **依據**：`agent-commons/capsules/TASK_S75_SIDEKICK_HUB.md` §6 驗收條件 + `~/.agentcharter/core/completion-delivery.md` §3 VCP 結構

---

## 📨 Directive Header

> **致 PM (Gemini CLI) / User**：以下 Phase 4 完工驗收測試計畫由 Engineer (Claude Code) 交付，請接手執行 ——
> Engineer 執行環境無法跨 session / 跨機驗證需 Blazor server + 瀏覽器 + Task Manager 的情境。
> 請依序執行 §1 → §7，把每段終端機輸出 **原文** 貼回此對話。Engineer 會判讀並決定是否關閉 S75 任務。
> 可選情境（標 🟡）失敗不阻擋驗收；核心情境（標 🔴）失敗即退稿。

**身份戳**（依 `~/.agentcharter/core/multi-role-tracking.md §3.2`）：
- 身份：Engineer (Claude Code / Opus 4.7 / 1M context)
- 切換情境：僅本 AI 扮演
- 本事件前一身份：無

**驗收標的**：
- inner CryptoBot/ commits: `c370032` (Phase 2 IPC) + `b3aacc5` (Bug 1 dispose fix) + `8bddbe1` (Phase 3 加固) — **已 push origin/main**
- outer agent-commons/ commits: `49c93c5` (Phase 1 schema probe report) — 已 push
- 既有測試: 26 Domain + 201 Application = **原 227** + Phase 4 新增 **7** = 共 234

---

## §1 Pre-flight（環境前置確認 / 📖 唯讀）

### 1.1 gemini CLI 環境

```powershell
gemini --version
# 預期：0.42.0

gemini --list-sessions | Select-Object -First 5
# 預期：列出歷史 sessions、證明 oauth-personal cached
```

### 1.2 inner CryptoBot/ commit 鏈確認

```powershell
cd D:\WorkSpace\AI_Lab\CryptoBot\CryptoBot
git log --oneline -5
```

**期望錨點**（依序，最新在上）：
```
8bddbe1 feat(s75): Phase 3 核心加固 — 429 retry (IRON ⑪ 雙保險) + Process 死偵測 ...
b3aacc5 fix(s69): BayesianSearchStrategy IAsyncDisposable dispose 例外 ...
c370032 feat(ai): S75 Phase 2 — gemini --acp 長連接 IPC 重構 (取代 -p single-shot)
44f4bd2 feat(ai): S74-C & S74-D — CryptoBot Sidekick + legacy cleanup
...
```

### 1.3 build 確認

```powershell
dotnet build CryptoBot.sln -c Debug
```

**期望錨點**：`建置成功。  0 個警告  0 個錯誤`

### 1.4 測試確認（234 全綠）

```powershell
dotnet test CryptoBot.sln -c Debug --no-build --nologo
```

**期望錨點**：
- `已通過! - 失敗: 0，通過: 26 ... CryptoBot.Domain.Tests.dll`
- `已通過! - 失敗: 0，通過: 208 ... CryptoBot.Application.Tests.dll`
- 總計 **234 全綠 / 0 失敗**（原 227 + Phase 4 新 7 個 `GlobalAiChatServiceTests`）

### 1.5 baseline process 計數

```powershell
Get-Process gemini, node, conhost -ErrorAction SilentlyContinue | Measure-Object | Select-Object Count
# 記下基準數字（用於 §6 對比）
```

---

## §2 情境 1 — ConsoleApp 啟動 + DI 驗證（📖 唯讀 / 🔴 必驗）

```powershell
cd D:\WorkSpace\AI_Lab\CryptoBot\CryptoBot
dotnet run --project src/CryptoBot.ConsoleApp -c Debug
```

**期望錨點**：
- ConsoleApp 啟動正常（無 unhandled exception）
- 出現 `Now listening on: http://localhost:5001`
- **不應**出現 `[GeminiAcp] 啟動 gemini --acp`（lazy init、首次 send 才觸發）
- **不應**出現 `InvalidOperationException: Unable to resolve service IGeminiAcpClient`

### 失敗解讀

| 看到 | 代表 | 修法 |
|---|---|---|
| DI resolution error | DI 註冊失效 | 退稿、Engineer 檢查 `DependencyInjection.cs:AddSidekick` |
| build error 即無法啟動 | 編譯問題 | 退稿、回 Phase 3 |

**通過後**保持 ConsoleApp 執行不關，繼續 §3-§5。

---

## §3 情境 2 — 第一輪對話（ACP Lazy Init + Session 建立）⚠️ 會建立 ACP 子 process / 🔴 必驗

開瀏覽器 `http://localhost:5001` → 點 Sidekick 側邊欄 → 發訊息：`請說 OK`

### 期望錨點（ConsoleApp log）

依序出現：
```
[GeminiAcp] 啟動 gemini --acp，cwd=C:\Users\<user>\AppData\Local\Temp\cryptobot-sidekick-<32-hex>
[GeminiAcp] initialize response received
[GeminiAcp] authenticate (oauth-personal) ok
[GeminiAcp] Session created: <UUID>
```

### 期望錨點（UI）

- Sidebar user 訊息「請說 OK」顯示
- AI 回應顯示（內容含 OK 字樣）
- 首輪延遲：~5-15s（含 ACP init + first prompt model latency）— 正常

### 期望錨點（Process）

新增至少 3 個 process（vs §1.5 baseline）：cmd / conhost / 2× node

```powershell
# 另開 PowerShell:
Get-Process gemini, node, conhost -ErrorAction SilentlyContinue | Select-Object Name, Id, WS, StartTime | Format-Table -AutoSize
```

### 失敗解讀

| 看到 | 代表 | 修法 |
|---|---|---|
| `Process.Start 回 false` | gemini 路徑找不到 | 檢查 `InteractiveCliAdvisorOptions.Executable` |
| Init 卡住 5 分鐘 | OAuth 未 cached | PM 端跑 `gemini login` |
| `⚠ gemini --acp 失敗` 訊息 | ACP 流程拋例外 | 貼錯誤完整訊息 |
| `429` / `RESOURCE_EXHAUSTED` log | model 滿載 | **Phase 3 應自動 retry 3 次**（exp backoff 1s/2s/4s）；觀察 log 是否含 `429 retry N/3, backoff Ms, method=...` |

---

## §4 情境 3 — 第 2+ 輪對話（**核心驗收：冷啟動消除**）⚠️ / 🔴

繼續同 Sidekick session、發訊息：`我剛才說了什麼？`

### 期望錨點（log）— **核心**

- **不應**再出現 `[GeminiAcp] 啟動 gemini --acp`（session 復用、process 不重起）
- **不應**再出現 `[GeminiAcp] Session created`

### 期望錨點（UI / 量測）— **目標重新評估**

> **校正**（依 user 2026-05-16 親跑 5 輪實測 + Phase 1 §11 ≤500ms 過於樂觀）：
> Capsule §6.1 原目標「第 2+ 輪 ≤ 500ms」**僅 cover「process spawn 省下」（~3s）**，
> 不能 cover model API latency（6-12s 為 Gemini model 自己的處理時間、ACP 無能為力）。
> **修正驗收標準**：第 2+ 輪「**無 ACP init log + 延遲 < 第 1 輪**」即視為冷啟動消除驗證通過。

### 期望錨點（Process）

對比 §3：process 數應**相同**（無新增、無減少）

### 失敗解讀（**核心**）

| 看到 | 代表 | 嚴重 |
|---|---|---|
| 第 2+ 輪 log 再出 `啟動 gemini --acp` | session/process 未復用 | 🔴 退稿、Phase 3 `EnsureSessionAsync` idempotency 失靈 |
| 第 2+ 輪延遲 ≥ 第 1 輪 | 冷啟動消除無效 | 🔴 同上 |
| 模型答不出前面內容 | session history 未持久 | 🟡 觀察項（gemini-cli ACP session 真實行為待確認、不阻擋）|

---

## §5 情境 4 — EMA SOL 長 prompt（Bug 2 false alarm 驗證 + stream 穩定）⚠️ / 🔴

> Bug 2 來源：user 2026-05-16 PM relay「S74 舊版 single-shot 處理長 prompt stream 卡死」、後續驗證為**非卡死**（model latency 137s、非 stream bug）。
> Phase 4 此 testcase：確認 ACP 模式下同 prompt **可完成 + stream 漸進顯示**。

### 操作

發以下完整 prompt（user 2026-05-16 提供原文、為 Phase 4 testcase payload）：

```
【當前決策模型】：EMA Trend Following

你是一位頂尖的加密貨幣量化交易分析師，請針對以下市場快照給出深度分析。

## 當前標的：SOL-USDT · 週期：OneHour
## 我目前在回測的策略模型：EMA Trend Following（EMA 黃金/死亡交叉 × RSI 動能）

## 技術面快照（最近 100 根 K 線計算得出）
- Close：89.2360
- 視窗漲跌幅 Δ：-8.32%
- RSI(14)：38.34
- ATR(14)：0.6389
- EMA(20)：89.9493
- EMA(50)：91.0977
- 布林帶位置 BB%：0.30（0 = 下軌、1 = 上軌）
- 系統初判趨勢：Downtrend

## 請回答
1. 綜合上述指標，目前市場屬於『上漲趨勢』『下跌趨勢』還是『區間震盪』？請說明依據。
2. 當前環境對我選的策略模型是利還是弊？如果不利，建議改用什麼風格（Trend Following / Mean Reversion / 按兵不動）？
3. 若要進場，建議的風險控制要點（停損位置、倉位比例、ATR 為倍數的止盈）是什麼？
4. 給出最值得掃描優化的 2~3 個參數維度（例如 RSI 門檻、BB StdDev、EMA 長度），並附建議的掃描範圍。
5. 請針對該模型的所有參數提供具體的優化參考值（包含 Min, Max, Step），以便我直接輸入回測系統掃描。

## 參數鍵名規範（硬性要求）
請使用以下標準鍵名提供建議數值：[FastEmaPeriod, SlowEmaPeriod, RsiPeriod, RsiMidline]
不得改名、不得縮寫、不得使用 snake_case；每個鍵對應 Min / Max / Step 三欄數值。
```

### 期望錨點

- **完成時間** ≤ 180s（user 2026-05-16 量測 137s、保留 ~30% buffer）
- AI 回應**不中斷**（stream chunks 連續到達、不卡在中段）
- 回應含 JSON code fence + 合法 keys（FastEmaPeriod / SlowEmaPeriod / RsiPeriod / RsiMidline）
- Sidebar 顯示「🪄 套用參數至實驗室」按鈕

### 失敗解讀

| 看到 | 代表 | 嚴重 |
|---|---|---|
| 真實卡死（無回應 > 200s）| stream 機制斷或 process hung | 🔴 退稿 |
| 「🪄 套用參數」按鈕不出現 | AiAdvicePayloadParser 整合斷 | 🔴 退稿 |
| 第 1 chunk 後一次性顯示（無漸進）| stream 解析 schema 不對 | 🟡 觀察、`HandleSessionUpdate` 內 `agent_message_chunk` schema 推導 fallback 路徑可能未命中、Phase 5 補完 |
| 429 retry log 出現 | model overload | ✅ 預期、Phase 3 retry 機制 work |

---

## §6 情境 5 — Bug 1 修法驗證（Bayesian dispose 不再拋例外）⚠️ / 🔴

> Bug 1 來源：user 2026-05-16 PM relay「Web UI 實驗室切到 Bayesian 模式後拋 IAsyncDisposable dispose 例外」、commit `b3aacc5` 修法。

### 操作

1. ConsoleApp 仍在跑（§2 後保持）
2. 開瀏覽器 → 實驗室 → 選任意策略 → SearchMethod 切到 **Bayesian** → 設 budget（如 3）→ 跑掃描

### 期望錨點（log）

- **不應**出現 `'BayesianSearchStrategy' 類型僅實作了 IAsyncDisposable 介面。請使用 DisposeAsync 來釋放容器`
- 掃描正常啟動（即使因 sidecar 未啟動而 fail，error 應為 `HttpRequestException` 或類似網路錯誤、**非 dispose 錯誤**）

### 失敗解讀

| 看到 | 代表 | 修法 |
|---|---|---|
| 仍出 IAsyncDisposable dispose 例外 | commit `b3aacc5` 未生效 | 重 build / 重 deploy ConsoleApp |
| 出 Python sidecar 連線錯誤（HttpRequestException）| 預期、本 testcase 只驗 dispose 不驗 sidecar | ✅ 接受、Phase 4 範圍不含 sidecar 啟動 |

---

## §7 情境 6 — Graceful Dispose / Process Tree Kill（🔴 核心）

### 操作

在 ConsoleApp window 按 **Ctrl+C**（非 Task Manager kill）— 走 .NET graceful shutdown、觸發 `IGeminiAcpClient.DisposeAsync()`

### 期望錨點（log）

可能任一：
- 無 dispose-related log（best case — stdin close → process 2s 內 clean exit）
- `[GeminiAcp] dispose: 清 temp dir ...` debug
- 🟡 警告：`[GeminiAcp] dispose: stdin close 後 2s 未 exit，強制 Kill(entireProcessTree:true)`

### 期望錨點（Process）— 核心

```powershell
Get-Process gemini, node, conhost -ErrorAction SilentlyContinue | Where-Object { $_.Id -in @(<§3 記下的 PID list>) }
# 預期：空（所有 ACP children 已死）
```

### 失敗解讀

| 看到 | 代表 | 嚴重 |
|---|---|---|
| 殘留 gemini / node | dispose tree kill 失敗 | 🔴 Phase 5 升級 Win32_Process recursive tree kill |
| 殘留 conhost | shim 未清理 | 🟡 觀察、不阻擋 |
| dispose 拋 Exception 進 log | `DisposeAsync` 路徑 bug | 🔴 退稿 |

**附註**：user 2026-05-16 已用 Stop-Process 強制殺 + 驗證 OS-level cleanup 通過（ACP children 隨 ConsoleApp 一起死）；本 §7 驗 **graceful dispose path** 為核心補完。

---

## §8 UI 視覺檢核 🟡

| 檢項 | 驗法 | 期望 |
|---|---|---|
| Sidekick 側邊欄持續顯示 | 任意頁面右邊應可見 | ✅ |
| 對話歷史跨頁面保留 | 切到 BacktestLab / 回首頁 → Sidebar 內容不丟 | ✅（Scoped per Blazor circuit、S74-C 既有）|
| 「🪄 套用參數」button | 含 JSON + 合法 keys 時應出現 | ✅ |
| 對話歷史清空 button | 清空 local history、ACP session 不變 | ✅ |

---

## §9 整體驗收判定表

| § | 情境 | 通過條件 | 失敗影響 |
|---|---|---|---|
| 1 | Pre-flight | 5/5 全綠（234 tests）| 🔴 退稿 |
| 2 | ConsoleApp 啟動 + DI | log + 監聽 port | 🔴 退稿 |
| 3 | 首輪對話 + ACP init | log 4 行 + UI 回應 + process 新增 | 🔴 退稿 |
| 4 | **冷啟動消除** | 無 init log + 延遲 < 首輪 | 🔴 退稿 |
| 5 | **EMA SOL 長 prompt** | ≤ 180s 完成 + JSON button | 🔴 退稿 |
| 6 | Bug 1 dispose 修法 | 不再拋 dispose 例外 | 🔴 退稿 |
| 7 | **Graceful dispose tree kill** | 0 殘留 ACP children | 🔴 退稿（Phase 5 補強）|
| 8 | UI 視覺 | 4/4 | 🟡 部分失敗可接受 |

**整體**：
- 全綠（§1-§7 全 🔴 通過 + §8 🟡 大多 OK）→ **S75 結案** + outer agent-commons/ 寫 HANDOFF_27 + 移檔到 archive
- §7 fail（dispose tree kill）→ Phase 5 升級 Win32_Process recursive tree kill 後再結案
- 其他 🔴 fail → 對應 Phase 退回重做

---

## §10 S75 commit chain summary

### outer agent-commons/（governance）
- `49c93c5` Phase 1 ACP schema probe report
- `8354c20` A009/A010 governance closure
- `9f5e4e9` revert（A010 audit trail）
- `67bb83b` A009 PM 違規 commit（已 revert）
- `671b426` Phase 0 capsule 立案

### inner CryptoBot/（src）
- `8bddbe1` Phase 3 核心加固（429 retry + Process 死偵測 + 429 user-facing）
- `b3aacc5` Bug 1 BayesianSearchStrategy dispose 修法
- `c370032` Phase 2 ACP IPC 重構

---

## §11 未完成項（不阻擋 S75 結案 / Phase 5 / 3.5 待辦）

| # | 項目 | 延後理由 | 觸發時機 |
|---|---|---|---|
| 1 | Fallback model chain | 需先補探 ACP `setModel` / `session/setModel` schema | Phase 3.5（schema 探完後） |
| 2 | Win32_Process recursive tree kill | §6 OS cleanup 已 partial 驗 + `Process.Kill(entireProcessTree:true)` 夠用 + 引入 `System.Management` 增加 Windows-only 平台綁定 | Phase 5（若 §7 出現殘留 process） |
| 3 | In-stream 429 retry | streaming + retry 互斥（C# yield 限制）+ channel reset 複雜度高 | Phase 5 評估 |
| 4 | `session/update` discriminator 完整列表親驗 | Phase 4 親驗 `agent_message_chunk` content schema fallback、其他 discriminator 為 best-effort | Phase 5 整合測試擴充 |
| 5 | `session/load` 跨 process 持久化機制 | Phase 1 §5 未親驗（schema 已知但持久化機制需追加實驗） | Phase 5 或場景觸發時 |

---

## §12 變更歷史

- **2026-05-17 v1.0** — Phase 4 完工 VCP 初版。涵蓋 7 情境（5 核心 + 2 非核心）+ Bug 1/2 驗證 testcase + EMA SOL 長 prompt payload + 234 tests 確認 + commit chain summary + 未完成項。對應 commit pending（待 user/PM 抽驗後寫 inner CryptoBot/ Phase 4 commit + outer agent-commons/ HANDOFF_27）。
